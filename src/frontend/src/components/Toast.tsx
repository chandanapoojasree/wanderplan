// Toast helper re-exports for consistent usage across pages
// Import from "sonner" directly for toast() calls, this is just a doc file.
// Usage: import { toast } from "sonner";
// toast.success("Trip saved!");
// toast.error("Something went wrong");
// toast("Info message");

export {}; // ensure module
